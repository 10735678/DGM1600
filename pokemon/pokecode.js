const pokeGrid = document.querySelector('pokeGrid')
const loadButton = document = document.querySelector('loadPokemon')

loadButton.addEventListener('click', () => {
    loadPage()
})

async function getAPIData(url) {
    try {

    } catch (error) {
        console.log(error)
    }
}

function loadPage() {

}